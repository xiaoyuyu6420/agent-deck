[package]
name = "agent-deck-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared types for Agent Deck host / UI / firmware"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
